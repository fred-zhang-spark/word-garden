# Word Garden 单词花园

一个给孩子用的"活的英语单词本"：孩子通过查询和拍照收集单词，通过自适应测验巩固记忆。

## 产品需求（来自家长，2026-08-28）

### 1. 收集单词的三种方式
- **查询收集**：孩子用中文（或其他语言）搜索一个词，得到英文释义/例句后，一键加入自己的单词本。
- **拍照识物**：对着任何实物拍照（好奇"这个东西英语叫什么"），AI 识别物体并给出英文单词，加入单词本。参考 iOS 应用 CapWords 的核心玩法。
- **拍纸质词表**：拍一张纸质单词表的照片，OCR 批量识别并导入单词本。

### 2. 测验与复习（本项目相对 CapWords 的增量功能）
- 针对当前单词本生成测验（quiz），帮助孩子记忆和复习。
- **自适应频率**：系统识别孩子不熟悉的单词（答错/犹豫的），提高这些词出现的频率；已掌握的词降低频率。可参考间隔重复（spaced repetition，如 SM-2/Leitner）思路，但交互要对儿童友好，不要像背单词软件那样枯燥。

### 3. 视觉风格
- 关键词：**artistic、cute、但不俗气（不要廉价幼稚感）**。
- 参考方向：CapWords 的云朵/贴纸风 UI，或 ChatGPT 那种干净留白的现代 UI。二选一或融合，先出风格小样给家长确认再铺开。

## 技术约定
- 目标形态：Web 应用（方便 iPad/iPhone/旧 MacBook 浏览器直接用，部署在家里 Mac mini 上）。优先做成 PWA，支持手机相机调用。
- 拍照识物/OCR/翻译需要调用 AI 视觉与语言能力：用 Claude API（模型用最新的，视觉用支持图像输入的模型）。
- **API 密钥绝不入库**：用 `.env` 存放，`.gitignore` 已包含。仓库是公开的。
- 单词本数据先用本地存储（浏览器 localStorage / 服务端 JSON 文件均可），起步不引入数据库，跑通再说。
- 用户是单个家庭的孩子，不需要账号系统；最多做一个"选择使用者"的简单切换。

## 工作方式
- 本项目由家长通过手机 Remote Control 远程指挥迭代，回复请用中文。
- 每完成一个可用的阶段性版本：`git add -A && git commit && git push`（远程仓库：fred-zhang-spark/word-garden，公开）。
- 风格样稿、重要 UI 决策先给家长看再继续。

## 已定决策

- **2026-08-28 视觉风格：选定方向 A「云朵贴纸」**（小样见 `docs/style-samples.html`）。
  雾蓝→暖米天空底色，白色贴纸卡 + 实心投影，圆体 Baloo 2 标题 + Nunito 正文；
  首页顶部是"会长苗的花园"（收几个词长几棵苗），复习叫"浇水"。
  饱和度压低，避开塑料玩具感。家长评价：对小朋友更亲切。
- **2026-08-28 目标形态：先 Web/PWA 跑通玩法，验证后再转原生 iOS App。**
  家长确认过：只要能持续累积数据就走 HTML。
- **2026-08-28 数据方案：服务端 JSON 文件（`data/garden.json`）为唯一真相源**，
  浏览器 localStorage 只做离线只读快照。原因：localStorage 会被"清浏览记录"、
  换浏览器、Safari 7 天未访问回收清掉，孩子攒的词不能冒这个险。
- **2026-08-28 访问方式：女儿在自己的 MacBook Air 上，经 Tailscale 访问 Mac mini。**
  服务监听 0.0.0.0；Mac mini 的 tailnet 地址是 `chriss-mac-mini.tail19f969.ts.net`
  （100.103.126.20）。要 PWA/离线就得上 HTTPS：`tailscale serve --bg 5173`。
- **2026-08-28 孩子年龄档：9-12 岁，已有基础**。可以出拼写题和例句填空。
- **2026-08-28 花园系统：植物由单词哈希生成**（`public/garden.js`）。
  品种/叶形/花色/果实/高矮/倾斜全部由单词决定，同一个词永远长同一个样。
  生长阶段 = Leitner 盒子 1-5：破土 → 幼苗 → 打苞 → 开花 → 结果。
  立体感用 SVG 做（固定右上光源、叶片双面、地面投影、三排空气透视），
  **不上 WebGL**——iPad/旧 MacBook 要跑得动，而且真 3D 容易做出塑料感。
  图鉴见 `docs/plant-codex.html`。
- **2026-08-28 AI 供应商将改为 OpenAI**（家长晚点提供 key）。
  `lib/ai.js` 是唯一的供应商适配层，换的时候只动这个文件。
- **贯穿要求：孩子易上手 + 粘性**。任何设计与实现决策都要先过这两关：
  孩子不用看字就知道点哪里；每次使用都有即时可见的回报。
